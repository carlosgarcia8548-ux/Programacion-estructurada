#include <stdio.h>
#include <stdlib.h>

int main()
{
////////Actividad 1
    int tablero[5][5];
    int numero[5][20]={1,2,3,4,5};
    char nombre[20];
    FILE *archivo;
    archivo = fopen("tablero.txt", "w");
    for(int i=0; i<5; i++){
        for(int j=0; j<5; j++){
            tablero[i][j] = '~';
        }
    }

tablero[1][2] = 'B';
tablero[3][4] = 'X';
tablero[2][1] = 'O';

    for(int i = 0; i < 5; i++){
        for(int j = 0; j < 5; j++){
            printf("%c ", tablero[i][j]);
        }
        printf("\n");
    }
//////////////-Archivo-///////////////
    for(int i = 0; i < 5; i++){
        for(int j = 0; j < 5; j++){
            fprintf(archivo, "%c ", tablero[i][j]);
        }
        fprintf(archivo, "\n");
    }
    fprintf(archivo, "Carlos Alberto\n");
    printf("Ingresa el nombre de un jugador");
    scanf("%s ", &nombre);
    printf("%s ", nombre);
    fprintf(archivo, "%s ", nombre);
    fclose(archivo);
    fopen("tablero.txt", "r");
////////Actividad 2
char bidimensional[4][20];
    int numero[5]={5,6,7,8,9};
    int numeros[5][20];
    char arregloC [5][20];
    for(int i=0; i<4; i++){
            printf("Ingresa tus nombres y apellidos: ");
            fgets(bidimensional[i],sizeof(bidimensional[i]), stdin);
    }
     for(int i=0; i<4; i++){
        printf("%s", bidimensional[i]);
     }
      for(int i=0; i<4; i++){
     printf("%d\n", &numero[i]);
      }
      for(int i=0; i<5; i++){
printf("%d\n", &arregloC[i]);
      }
printf("%d\n", arregloC[6]);

for(int i=0; i<5; i++){
    scanf("%d\n", &numeros[i]);
}
for(int i=0; i<5; i++){
    printf("%d->%d\n", &numeros[i], numeros[i]);
}

    return 0;
}
