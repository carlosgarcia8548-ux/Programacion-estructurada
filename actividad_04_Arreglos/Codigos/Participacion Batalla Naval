#include <stdio.h>

int main() {
////////Actividad 1
char jugador1[20], jugador2[20];
int tablero[5][5], tablero2[5][5];
int barco1, barco2, barco3, barco4, barco5, barco6;
char barco[3][20];
char barcob[3][20];
int fila, columna;
int contador=0, contador2=0;
//Iniciar jugador
printf("Ingrese el nombre del jugador 1: ");
fgets(jugador1, sizeof(jugador1), stdin);
printf("Bienvenido jugador 1: %s", jugador1);
// Inicializar tablero
printf("Tablero de %s\n", jugador1);
for(int i = 0; i < 5; i++) {
    for(int j = 0; j < 5; j++) {
        tablero[i][j] = 0;
        printf("%d", tablero[i][j]);
    }printf("\n\n");
}
////////Jugador 2////////
printf("Ingrese el nombre del jugador 2: ");
fgets(jugador2, sizeof(jugador2), stdin);
printf("Bienvenido jugador 2: %s", jugador2);
printf("Tablero de %s\n", jugador2);
for(int i = 0; i < 5; i++) {
    for(int j = 0; j < 5; j++) {
        tablero2[i][j] = 0;
        printf("%d", tablero2[i][j]);
    }printf("\n\n");
}
// Colocar barcos manualmente
for(int i=0; i<3; i++){
    printf("Ingresa la cordenada donde quieres agregar tus barcos %s\n", jugador1);
    printf("Fila: ");
    scanf("%d", &fila);
    printf("Columna: ");
    scanf("%d", &columna);
    if(tablero[fila][columna]==1){
        printf("La cordenada ya existe\n");
        i=i-1;
    }else{
        tablero[fila][columna] = 1;
            if(i==0){
                barco1=tablero[fila][columna];
            }else if(i==1){
                barco2=tablero[fila][columna];
            }else if(i==2){
                barco3=tablero[fila][columna];
                }
        printf("Agrega el nombre del barco: ");
            scanf("%s", barco[i]);
        printf("El barco se llama: %s\n", barco[i]);
    }
 }
 ////////Jugador 2////////
 for(int i=0; i<3; i++){
    printf("Ingresa la cordenada donde quieres agregar tus barcos %s\n", jugador2);
    printf("Fila: ");
    scanf("%d", &fila);
    printf("Columna: ");
    scanf("%d", &columna);
    if(tablero2[fila][columna]==1){
        printf("La cordenada ya existe\n");
        i=i-1;
    }else{
        tablero2[fila][columna]=1;
            if(i==0){
                barco4=tablero2[fila][columna];
            }else if(i==1){
                barco5=tablero2[fila][columna];
            }else if(i==2){
                barco6=tablero2[fila][columna];
            }
        printf("Agrega el nombre del barco: ");
            scanf("%s", barcob[i]);
        printf("El barco se llama: %s\n", barcob[i]);
    }
 }
// Mostrar tablero con los barcos
printf("Tablero 1\n");
for(int i = 0; i < 5; i++) {
    for(int j = 0; j < 5; j++) {
        printf("%d", tablero[i][j]);
    }
printf("\n\n");
}
 ////////Jugador 2////////
 printf("Tablero 2\n");
 for(int i = 0; i < 5; i++) {
    for(int j = 0; j < 5; j++) {
        printf("%d", tablero2[i][j]);
    }
printf("\n\n");
}
// Pedir disparo
printf("-----Tendran 5 oportunidades de disparo-----\n");
for(int l=0; l<5; l++){
printf("---Disparo-----\n");
printf("Ingresa la cordenada que quieres atacar %s\n", jugador1);
printf("Fila: ");
scanf("%d", &fila);

printf("Columna: ");
scanf("%d", &columna);
// AQUÍ DEBES COMPLETAR:
// evaluar si hay barco o agua
        tablero2[fila][columna];
        if(tablero2[fila][columna]==1){
                if(tablero2[fila][columna]==barco4){
                    printf("%sle diste al barco %s\n", jugador1, barcob[0]);
                    barco4=3;
                }else if(tablero2[fila][columna]==barco5){
                    printf("%sle diste al barco %s\n", jugador1, barcob[1]);
                    barco5=3;
                }else if(tablero2[fila][columna]==barco6){
                    printf("%sle diste al barco %s\n", jugador1, barcob[2]);
                    barco6=3;
                }
            contador= contador + 1;
            tablero2[fila][columna]=3;
        }else{
        printf("Impacto fallido\n");
        tablero2[fila][columna]=2;
        }
for(int i = 0; i < 5; i++) {
    for(int j = 0; j < 5; j++) {
        printf("%d", tablero2[i][j]);
    }
printf("\n\n");
}
 if(barco4 == 3 && barco5 == 3 && barco6 == 3){
            printf("Ganaste el juego %s\n", jugador1);
            for(int i = 0; i < 5; i++) {
                for(int j = 0; j < 5; j++) {
                    printf("%d", tablero[i][j]);
                }
            printf("\n\n");
}
printf("Jugador %sle diste a %d barcos\n", jugador1, contador);
printf("Jugador %sle diste a %d barcos\n", jugador2, contador2);
            return 0;
        }
/////-Jugador 2-//////////
printf("---Disparo-----\n");
printf("Ingresa la cordenada que quieres atacar %s\n", jugador2);
printf("Fila: ");
scanf("%d", &fila);

printf("Columna: ");
scanf("%d", &columna);
// AQUÍ DEBES COMPLETAR:
// evaluar si hay barco o agua
        tablero[fila][columna];
        if(tablero[fila][columna]==1){
                if(tablero[fila][columna]==barco1){
                    printf("%sle diste al barco %s\n", jugador2, barco[0]);
                    barco1=3;
                }else if(tablero[fila][columna]==barco2){
                    printf("%sle diste al barco %s\n", jugador2, barco[1]);
                    barco2=3;
                }else if(tablero[fila][columna]==barco3){
                    printf("%sle diste al barco %s\n", jugador2, barco[2]);
                    barco3=3;
                }
            contador2= contador2 + 1;
            tablero[fila][columna]=3;
        }else{
        printf("Impacto fallido\n");
        tablero[fila][columna]=2;
        }
for(int i = 0; i < 5; i++) {
    for(int j = 0; j < 5; j++) {
        printf("%d", tablero[i][j]);
    }
printf("\n\n");
}
 if(barco1 == 3 && barco2 == 3 && barco3 == 3){
            printf("Ganaste el juego %s\n", jugador2);
            for(int i = 0; i < 5; i++) {
                for(int j = 0; j < 5; j++) {
                    printf("%d", tablero[i][j]);
                }
            printf("\n\n");
}
printf("Jugador %sle diste a %d barcos\n", jugador1, contador);
printf("Jugador %sle diste a %d \n", jugador2, contador2);
            return 0;
        }
}
printf("Jugador %sle diste a %d barcos\n", jugador1, contador);
printf("Jugador %sle diste a %d barcos\n", jugador2, contador2);

////////Actividad 2

return 0;
}
