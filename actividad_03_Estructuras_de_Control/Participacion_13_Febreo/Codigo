#include <stdio.h>
#include <stdlib.h>
int main() {
/////Actividad 1
    int a, b;
    printf("Ingresa tu calificación\n");
    scanf("%d", &a);
    printf("La cantidad de materias reprobadas\n");
    scanf("%d", &b);
    if(a >= 85 && b == 0)
        printf("Puedes obtener la beca\n");
    else
        printf("No puedes obtener la beca\n");
/////Actividad 2
     int e;
    printf("Ingresa tu edad");
    scanf("%d", &e);
    if(e>=18){
         printf("Es mayor de edad");
    }else{
    printf("No es mayor de edad");
    }
/////Actividad 3
int a;
    printf("Ingresa un numero");
    scanf("%d", &a);

    if(a % 2 == 0)
        printf("Es par");
    else
        printf("No es par");
/////Actividad 4
    int p;
    printf("Ingresa un numero");
    scanf("%d", &p);
    if((p%2==0 && p!=2)||(p%3==0 && p!=3)||(p%5==0 && p!=5)){
        printf("No es un numero Par");
    }else{
    printf("Es un numero Par");
    }
/////Actividad 5
int a, c, d;
    float b;
    printf("Ingresa tu edad, tu calificación y la cantidad de materias reprobadas\n");
    scanf("%d %f %d", &a, &b, &c);

    if(a >= 18){
        if(b >= 9.0 && c == 0)
            printf("Puedes obtener la beca");
        else{
            if(c <= 2)
                printf("Puede reinscribirse");
            else{
                if(c > 3)
                    printf("Esta en situación crítica");
                else
                    printf("No puedes aplicar a la beca");
            }
        }
    }
    else
        printf("No puedes aplicar a la beca");
/////Actividad 6
    int a;
    printf("Ingresa un numero");
    scanf("%d", &a);
    if(a == 0){
        printf("Es impar");
    }else{
        printf("No es impar");
    }
    return 0;
}
